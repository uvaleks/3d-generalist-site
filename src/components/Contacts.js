function Contacts({children}) {

    return (
        <>
            <section className="contacts">
                {children}
            </section>
        </>
    );
  }
  
  export default Contacts;